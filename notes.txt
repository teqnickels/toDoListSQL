- task file will sort commands to functions that will change data base
    -  add will INSERT INTO table 
    -  delete will remove a task from table
    -  complete will change the completion status of a task
    -  list will select all the task names and ids and return them to us

-  Handle how console/status displays to us 
    - design a function that will handle how to respond to the console.

- Handle accessing the database for information
    -the switch case in the tasks.js file will call the routes, the routes will execite the promises
    which hold the sql calls

-   the schema will detail a tasks table in our db. tasks will have an:
    -   id which is auto incrementing
    -   task VARCHAR 200
    -   status (completed or nah) BOOLEAN
